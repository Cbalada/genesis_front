'use client'

import { useEffect, useMemo, useState } from 'react'
import { ArrowRight, CalendarDays, ChevronDown, Globe2, Heart, Menu, Search, SlidersHorizontal, Star, Users, X } from 'lucide-react'
import { api, type AdminStats, type Booking, type Property, type Review, type User, type UserRole } from '@/lib/api'

const fallback: Property[] = [
 { id:'1', title:'Casa de diseño frente al lago', city:'Villa La Angostura', country:'Argentina', pricePerNight:184, maxGuests:6, bedrooms:3, bathrooms:2, propertyType:'HOUSE', description:'Casa equipada para descansar frente al lago.', images:[{imageUrl:'https://images.unsplash.com/photo-1449158743715-0a90ebb6d2d8?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
 { id:'2', title:'Refugio entre viñedos', city:'Luján de Cuyo, Mendoza', country:'Argentina', pricePerNight:126, maxGuests:4, bedrooms:2, bathrooms:1, propertyType:'HOUSE', description:'Refugio tranquilo entre viñedos.', images:[{imageUrl:'https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
 { id:'3', title:'Loft luminoso en Palermo', city:'Buenos Aires', country:'Argentina', pricePerNight:92, maxGuests:3, bedrooms:1, bathrooms:1, propertyType:'APARTMENT', description:'Loft cómodo con excelente ubicación.', images:[{imageUrl:'https://images.unsplash.com/photo-1493809842364-78817add7ffb?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
 { id:'4', title:'Cabaña nórdica en el bosque', city:'San Martín de los Andes', country:'Argentina', pricePerNight:148, maxGuests:5, bedrooms:2, bathrooms:1, propertyType:'HOUSE', description:'Cabaña rodeada de bosque.', images:[{imageUrl:'https://images.unsplash.com/photo-1510798831971-661eb04b3739?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
 { id:'5', title:'Casa cálida junto al mar', city:'Mar del Plata', country:'Argentina', pricePerNight:108, maxGuests:5, bedrooms:2, bathrooms:2, propertyType:'HOUSE', description:'Casa familiar cerca del mar.', images:[{imageUrl:'https://images.unsplash.com/photo-1499793983690-e29da59ef1c2?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
 { id:'6', title:'Departamento con terraza', city:'Córdoba', country:'Argentina', pricePerNight:74, maxGuests:4, bedrooms:2, bathrooms:1, propertyType:'APARTMENT', description:'Departamento con terraza privada.', images:[{imageUrl:'https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?auto=format&fit=crop&w=1200&q=85', isCover:true}] },
]
const categories = [['Todo',''],['Casas','HOUSE'],['Cabañas','HOUSE'],['Departamentos','APARTMENT'],['Habitaciones','ROOM'],['Hoteles','HOTEL']]
const storageKey = 'genesis.session'
const img = (p: Property) => p.images?.find(i=>i.isCover)?.imageUrl || p.images?.[0]?.imageUrl || fallback[0].images![0].imageUrl
const money = (value: string | number | undefined) => Number(value || 0).toLocaleString('es-AR')
const message = (error: unknown) => error instanceof Error ? error.message : 'No pudimos completar la operación.'

type Session = { token: string; user: User }

export default function Page() {
 const [properties,setProperties] = useState<Property[]>(fallback)
 const [destination,setDestination] = useState(''), [category,setCategory] = useState(''), [guests,setGuests] = useState(2), [showGuests,setShowGuests] = useState(false)
 const [checkIn,setCheckIn] = useState(''), [checkOut,setCheckOut] = useState('')
 const [favorites,setFavorites] = useState<string[]>([]), [selected,setSelected] = useState<Property|null>(null), [auth,setAuth] = useState(false), [host,setHost] = useState(false), [menu,setMenu] = useState(false)
 const [bookingsOpen,setBookingsOpen] = useState(false), [adminOpen,setAdminOpen] = useState(false)
 const [user,setUser] = useState<User|null>(null), [token,setToken] = useState('')
 const [notice,setNotice] = useState(''), [loading,setLoading] = useState(true), [busy,setBusy] = useState('')
 const visible = useMemo(()=>properties.filter(p=>(!category || p.propertyType===category)),[properties,category])
 const isHost = user?.role === 'HOST' || user?.role === 'ADMIN'

 useEffect(()=>{ restoreSession(); search(true) }, [])
 useEffect(()=>{ if(token) syncFavorites(token) }, [token])

 async function restoreSession(){
  const raw = localStorage.getItem(storageKey)
  if(!raw){ setLoading(false); return }
  try { const saved = JSON.parse(raw) as Session; const current = await api.me(saved.token); setToken(saved.token); setUser(current); localStorage.setItem(storageKey, JSON.stringify({token:saved.token,user:current})) }
  catch { localStorage.removeItem(storageKey); setToken(''); setUser(null) }
  finally { setLoading(false) }
 }
 async function search(silent=false, nextCategory=category){
  const q=new URLSearchParams({page:'1',limit:'20',guests:String(guests)})
  if(destination.trim()) q.set('city',destination.trim())
  if(nextCategory) q.set('propertyType',nextCategory)
  setBusy('search'); if(!silent) setNotice('')
  try { const r=await api.listProperties(q); setProperties(r.data); if(!r.data.length) setNotice('No encontramos alojamientos con esos filtros.') }
  catch { if(!silent) setNotice('Mostrando una selección mientras conectamos con Genesis Rentals.'); setProperties(fallback) }
  finally { setBusy('') }
 }
 function saveSession(next: Session){ setToken(next.token); setUser(next.user); localStorage.setItem(storageKey, JSON.stringify(next)); syncFavorites(next.token) }
 function logout(){ setToken(''); setUser(null); setFavorites([]); localStorage.removeItem(storageKey); setMenu(false); setNotice('Sesión cerrada correctamente.') }
 async function syncFavorites(authToken=token){ try { const r=await api.favorites(authToken); setFavorites(r.data.map(f=>f.propertyId)) } catch {} }
 async function toggleFavorite(id:string){
  if(!user || !token){setAuth(true);return}
  const wasFavorite = favorites.includes(id)
  setFavorites(f=>wasFavorite?f.filter(x=>x!==id):[...f,id])
  try { if(wasFavorite) await api.removeFavorite(token,id); else await api.addFavorite(token,id) }
  catch(error){ setFavorites(f=>wasFavorite?[...f,id]:f.filter(x=>x!==id)); setNotice(message(error)) }
 }
 async function openProperty(property: Property){
  setSelected(property)
  try { setSelected(await api.getProperty(property.id)) } catch {}
 }
 return <main className="min-h-screen bg-background text-foreground">
  <header className="sticky top-0 z-30 border-b border-border bg-background/95 backdrop-blur"><div className="mx-auto flex max-w-[1440px] items-center justify-between px-5 py-4 lg:px-10"><a href="#inicio" className="flex items-center gap-2"><span className="flex size-9 items-center justify-center rounded-xl bg-primary font-black text-primary-foreground">G</span><b className="text-xl tracking-tight">genesis<span className="text-primary">.</span></b></a><nav className="hidden gap-8 text-sm font-medium text-muted-foreground md:flex"><a href="#alojamientos" className="text-foreground">Alojamientos</a><a href="#inspiracion">Inspiración</a><a href="#anfitriones">Para anfitriones</a></nav><div className="flex items-center gap-2"><button onClick={()=>user?setMenu(!menu):setAuth(true)} className="hidden rounded-full px-4 py-2 text-sm font-semibold hover:bg-muted sm:block">{loading?'Verificando...':user?.name || 'Iniciar sesión'}</button><button className="flex size-10 items-center justify-center rounded-full border border-border"><Globe2 size={17}/></button><button onClick={()=>setMenu(!menu)} className="flex size-10 items-center justify-center rounded-full border border-border"><Menu size={18}/></button>{menu&&<div className="absolute right-5 top-16 w-56 rounded-2xl border border-border bg-card p-2 shadow-xl"><button onClick={()=>user?logout():setAuth(true)} className="w-full rounded-xl px-3 py-2 text-left text-sm hover:bg-muted">{user?'Cerrar sesión':'Iniciar sesión'}</button><button onClick={()=>{setHost(true);setMenu(false)}} className="w-full rounded-xl px-3 py-2 text-left text-sm hover:bg-muted">Publicar alojamiento</button><button onClick={()=>{user?setBookingsOpen(true):setAuth(true);setMenu(false)}} className="w-full rounded-xl px-3 py-2 text-left text-sm hover:bg-muted">Mis reservas</button>{user?.role==='ADMIN'&&<button onClick={()=>{setAdminOpen(true);setMenu(false)}} className="w-full rounded-xl px-3 py-2 text-left text-sm hover:bg-muted">Panel admin</button>}</div>}</div></div><div className="mx-auto max-w-4xl px-5 pb-4 lg:px-10"><div className="flex flex-col rounded-2xl border border-border bg-card p-2 shadow-sm md:flex-row"><label className="flex flex-1 flex-col gap-1 rounded-xl px-4 py-2 text-xs font-semibold hover:bg-muted">¿A dónde vas?<input value={destination} onChange={e=>setDestination(e.target.value)} placeholder="Explorá destinos" className="bg-transparent text-sm font-normal outline-none"/></label><div className="grid flex-1 grid-cols-2 gap-1 rounded-xl px-4 py-2 text-xs font-semibold hover:bg-muted"><label><span className="flex items-center gap-2"><CalendarDays size={14}/> Entrada</span><input value={checkIn} onChange={e=>setCheckIn(e.target.value)} type="date" className="mt-1 w-full bg-transparent text-sm font-normal text-muted-foreground outline-none"/></label><label><span>Salida</span><input value={checkOut} onChange={e=>setCheckOut(e.target.value)} type="date" className="mt-1 w-full bg-transparent text-sm font-normal text-muted-foreground outline-none"/></label></div><div className="relative flex flex-1"><button onClick={()=>setShowGuests(!showGuests)} className="flex flex-1 flex-col gap-1 rounded-xl px-4 py-2 text-left text-xs font-semibold hover:bg-muted"><span className="flex items-center gap-2"><Users size={14}/> Viajeros</span><span className="text-sm font-normal text-muted-foreground">{guests} viajeros</span></button>{showGuests&&<div className="absolute right-0 top-16 z-40 w-56 rounded-2xl border border-border bg-card p-4 shadow-xl"><div className="flex items-center justify-between"><span className="text-sm">Adultos</span><div className="flex items-center gap-3"><button onClick={()=>setGuests(Math.max(1,guests-1))} className="size-8 rounded-full border">-</button><span>{guests}</span><button onClick={()=>setGuests(guests+1)} className="size-8 rounded-full border">+</button></div></div></div>}<button disabled={busy==='search'} onClick={()=>search()} className="flex size-12 items-center justify-center rounded-xl bg-primary text-primary-foreground disabled:opacity-60" aria-label="Buscar"><Search size={19}/></button></div></div></div></header>
  <section id="inicio" className="mx-auto max-w-[1440px] px-5 pb-10 pt-10 lg:px-10 lg:pt-14"><div className="grid items-center gap-8 lg:grid-cols-[1fr_1.05fr]"><div className="max-w-xl"><p className="mb-5 text-sm font-semibold uppercase tracking-[.18em] text-primary">Tu próxima historia empieza acá</p><h1 className="text-balance text-5xl font-semibold leading-[1.02] tracking-[-.045em] sm:text-6xl lg:text-7xl">Encontrá un lugar para <span className="text-primary">sentirte en casa.</span></h1><p className="mt-6 max-w-md text-base leading-7 text-muted-foreground">Alojamientos únicos, anfitriones reales y destinos que se quedan con vos mucho después de volver.</p><div className="mt-8 flex flex-wrap gap-3"><a href="#alojamientos" className="flex items-center gap-2 rounded-full bg-primary px-6 py-3.5 text-sm font-semibold text-primary-foreground">Explorar alojamientos <ArrowRight size={16}/></a><button onClick={()=>setHost(true)} className="rounded-full border border-border px-6 py-3.5 text-sm font-semibold hover:bg-muted">Publicá tu espacio</button></div><div className="mt-10 flex gap-6 text-sm"><div><strong className="text-lg">+12k</strong><span className="ml-2 text-muted-foreground">alojamientos</span></div><div className="h-8 w-px bg-border"/><div><strong className="text-lg">4.9</strong><span className="ml-2 text-muted-foreground">valoración media</span></div></div></div><div className="relative overflow-hidden rounded-[2rem]"><img src="https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1600&q=90" alt="Casa junto a un lago" className="h-[420px] w-full object-cover sm:h-[520px]"/><div className="absolute bottom-5 left-5 right-5 flex items-end justify-between rounded-2xl bg-primary/90 p-4 text-primary-foreground"><div><p className="text-xs opacity-75">Escapada destacada</p><p className="mt-1 font-semibold">Patagonia, Argentina</p></div><span className="rounded-full bg-primary-foreground/15 px-3 py-1.5 text-xs">Desde $98 / noche</span></div></div></div></section>
  <section id="alojamientos" className="mx-auto max-w-[1440px] px-5 py-8 lg:px-10"><div className="flex flex-col gap-5 border-b border-border pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-primary">Para vos</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Descubrí tu próxima escapada</h2></div><button onClick={()=>search()} className="flex items-center gap-2 self-start rounded-full border border-border px-4 py-2.5 text-sm font-medium hover:bg-muted"><SlidersHorizontal size={16}/> Filtros</button></div><div className="flex gap-6 overflow-x-auto py-5">{categories.map(([label,value])=><button key={label} onClick={()=>{setCategory(value); search(true, value)}} className={`shrink-0 border-b-2 pb-2 text-sm font-medium ${category===value?'border-primary':'border-transparent text-muted-foreground'}`}>{label}</button>)}</div>{notice&&<div className="mb-5 rounded-xl bg-secondary px-4 py-3 text-sm text-secondary-foreground">{notice}</div>}<div className="grid gap-x-5 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{visible.map(p=><article key={p.id} className="group cursor-pointer" onClick={()=>openProperty(p)}><div className="relative overflow-hidden rounded-2xl bg-muted"><img src={img(p)} alt={p.title} className="aspect-[1.18] w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute left-3 top-3 rounded-full bg-card/95 px-3 py-1.5 text-xs font-semibold">{p.propertyType==='APARTMENT'?'Gran ubicación':'Favorito entre huéspedes'}</div><button onClick={e=>{e.stopPropagation();toggleFavorite(p.id)}} disabled={busy===`fav-${p.id}`} className="absolute right-3 top-3 flex size-9 items-center justify-center rounded-full bg-card/90 disabled:opacity-60" aria-label="Favorito"><Heart size={17} fill={favorites.includes(p.id)?'currentColor':'none'} className={favorites.includes(p.id)?'text-primary':''}/></button></div><div className="pt-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold leading-5">{p.title}</h3><p className="mt-1 text-sm text-muted-foreground">{p.city}, {p.country}</p></div><span className="flex items-center gap-1 text-sm"><Star size={14} fill="currentColor"/> 4.9</span></div><p className="mt-2 text-sm text-muted-foreground">{p.bedrooms} habitaciones · hasta {p.maxGuests} huéspedes</p><p className="mt-2 text-sm"><strong>${money(p.pricePerNight)}</strong> noche</p></div></article>)}</div>{!visible.length&&<div className="rounded-2xl bg-muted p-10 text-center text-muted-foreground">No encontramos alojamientos con esos filtros.</div>}</section>
  <section id="inspiracion" className="mx-auto max-w-[1440px] px-5 py-16 lg:px-10"><div className="grid gap-6 rounded-[2rem] bg-secondary p-8 md:grid-cols-[1fr_auto] md:items-center md:p-12"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-primary">Viajá distinto</p><h2 className="mt-3 max-w-xl text-3xl font-semibold tracking-tight sm:text-4xl">Más que un alojamiento, un recuerdo para llevarte.</h2><p className="mt-4 max-w-lg leading-7 text-muted-foreground">Cada estadía tiene una historia. Encontrá la tuya con anfitriones que conocen el lugar como nadie.</p></div><a href="#alojamientos" className="flex items-center gap-2 rounded-full bg-primary px-6 py-3.5 text-sm font-semibold text-primary-foreground">Ver inspiración <ArrowRight size={16}/></a></div></section>
  <footer id="anfitriones" className="border-t border-border"><div className="mx-auto flex max-w-[1440px] flex-col gap-6 px-5 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-10"><b className="text-foreground">genesis.</b><span>© 2026 Genesis Rentals</span><button onClick={()=>setHost(true)} className="font-medium text-foreground hover:text-primary">Convertite en anfitrión</button></div></footer>
  {selected&&<PropertyModal property={selected} user={user} token={token} checkIn={checkIn} checkOut={checkOut} guests={guests} onClose={()=>setSelected(null)} onAuth={()=>setAuth(true)} onNotice={setNotice}/>} 
  {auth&&<AuthModal onClose={()=>setAuth(false)} onLogin={(session)=>{saveSession(session);setAuth(false)}}/>}
  {host&&<HostModal onClose={()=>setHost(false)} user={user} token={token} onAuth={()=>setAuth(true)} onCreated={(p)=>{setProperties(list=>[p,...list]);setNotice('Alojamiento publicado correctamente.')}}/>}
  {bookingsOpen&&<BookingsModal onClose={()=>setBookingsOpen(false)} token={token} onAuth={()=>setAuth(true)}/>} 
  {adminOpen&&<AdminModal onClose={()=>setAdminOpen(false)} token={token} user={user}/>} 
 </main>
}

function PropertyModal({property,user,token,checkIn,checkOut,guests,onClose,onAuth,onNotice}:{property:Property;user:User|null;token:string;checkIn:string;checkOut:string;guests:number;onClose:()=>void;onAuth:()=>void;onNotice:(v:string)=>void}){
 const [inDate,setInDate]=useState(checkIn), [outDate,setOutDate]=useState(checkOut), [count,setCount]=useState(guests), [busy,setBusy]=useState(false), [error,setError]=useState(''), [created,setCreated]=useState<Booking|null>(null), [reviews,setReviews]=useState<Review[]>([])
 useEffect(()=>{ api.propertyReviews(property.id).then(r=>setReviews(r.data)).catch(()=>setReviews([])) }, [property.id])
 async function reserve(){
  if(!user || !token){onAuth();return}
  if(!inDate || !outDate){setError('Elegí fecha de entrada y salida.');return}
  setBusy(true); setError('')
  try { const booking = await api.createBooking(token,{propertyId:property.id,checkIn:inDate,checkOut:outDate,guests:count}); setCreated(booking); onNotice('Reserva creada correctamente.') }
  catch(error){ setError(message(error)) }
  finally { setBusy(false) }
 }
 return <div className="fixed inset-0 z-50 flex items-end justify-center bg-foreground/40 p-0 sm:items-center sm:p-6" onClick={onClose}><div onClick={e=>e.stopPropagation()} className="max-h-[90vh] w-full max-w-2xl overflow-auto rounded-t-3xl bg-card sm:rounded-3xl"><div className="relative"><img src={img(property)} alt={property.title} className="h-64 w-full object-cover sm:h-80"/><button onClick={onClose} className="absolute right-4 top-4 flex size-10 items-center justify-center rounded-full bg-card/90"><X size={18}/></button></div><div className="flex flex-col gap-5 p-6"><div><p className="text-sm text-muted-foreground">{property.city}, {property.country}</p><h2 className="mt-1 text-2xl font-semibold">{property.title}</h2><p className="mt-2 text-sm text-muted-foreground">{property.bedrooms} habitaciones · {property.bathrooms} baños · hasta {property.maxGuests} huéspedes</p></div><p className="leading-7 text-muted-foreground">{property.description || 'Un espacio pensado para descansar, descubrir el destino y vivir una estadía memorable con todo lo necesario.'}</p><div className="grid gap-3 border-t border-border pt-5 sm:grid-cols-3"><label className="text-sm font-medium">Entrada<input value={inDate} onChange={e=>setInDate(e.target.value)} type="date" className="mt-2 w-full rounded-xl border border-input bg-background px-3 py-2 outline-none"/></label><label className="text-sm font-medium">Salida<input value={outDate} onChange={e=>setOutDate(e.target.value)} type="date" className="mt-2 w-full rounded-xl border border-input bg-background px-3 py-2 outline-none"/></label><label className="text-sm font-medium">Huéspedes<input value={count} min={1} max={property.maxGuests} onChange={e=>setCount(Number(e.target.value))} type="number" className="mt-2 w-full rounded-xl border border-input bg-background px-3 py-2 outline-none"/></label></div>{error&&<p className="rounded-xl bg-secondary px-4 py-3 text-sm text-destructive">{error}</p>}{created&&<p className="rounded-xl bg-secondary px-4 py-3 text-sm">Reserva {created.status}. Total: ${money(created.totalPrice)}</p>}<div className="flex items-center justify-between"><span><b className="text-xl">${money(property.pricePerNight)}</b> / noche</span><button disabled={busy} onClick={reserve} className="rounded-full bg-primary px-6 py-3 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Reservando...':'Reservar'}</button></div><div className="border-t border-border pt-5"><h3 className="font-semibold">Reviews</h3>{reviews.length?<div className="mt-3 space-y-3">{reviews.map(r=><div key={r.id} className="rounded-xl bg-secondary p-4 text-sm"><div className="flex justify-between"><b>{r.user?.name || 'Huésped'}</b><span>{r.rating}/5</span></div><p className="mt-1 text-muted-foreground">{r.comment}</p></div>)}</div>:<p className="mt-2 text-sm text-muted-foreground">Todavía no hay reviews para este alojamiento.</p>}</div></div></div></div>
}

function AuthModal({onClose,onLogin}:{onClose:()=>void;onLogin:(s:Session)=>void}){const [mode,setMode]=useState<'login'|'register'>('login'),[name,setName]=useState(''),[email,setEmail]=useState('guest@genesis.com'),[password,setPassword]=useState('Password123!'),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(){setBusy(true);setError('');try{const r=mode==='login'?await api.login({email,password}):await api.register({name,email,password});onLogin({token:r.accessToken,user:r.user})}catch(error){setError(message(error))}finally{setBusy(false)}}return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-foreground/40 p-5"><div className="w-full max-w-md rounded-3xl bg-card p-7"><div className="flex justify-between"><div><p className="text-sm font-semibold text-primary">Bienvenido a genesis.</p><h2 className="mt-1 text-2xl font-semibold">{mode==='login'?'Iniciá sesión':'Creá tu cuenta'}</h2></div><button onClick={onClose}><X/></button></div><div className="mt-6 flex flex-col gap-4">{mode==='register'&&<label className="text-sm font-medium">Nombre<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none"/></label>}<label className="text-sm font-medium">Email<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none" type="email"/></label><label className="text-sm font-medium">Contraseña<input value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 outline-none" type="password"/></label>{error&&<p className="text-sm text-destructive">{error}</p>}<button disabled={busy} onClick={submit} className="rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Conectando...':'Continuar'}</button><button onClick={()=>setMode(mode==='login'?'register':'login')} className="text-center text-sm text-muted-foreground hover:text-foreground">{mode==='login'?'Crear cuenta':'Ya tengo cuenta'}</button><p className="text-center text-xs text-muted-foreground">Demo API: guest@genesis.com · Password123!</p></div></div></div>}

function HostModal({onClose,user,token,onAuth,onCreated}:{onClose:()=>void;user:User|null;token:string;onAuth:()=>void;onCreated:(p:Property)=>void}){const [form,setForm]=useState({title:'',city:'',country:'Argentina',pricePerNight:'',maxGuests:'2'}),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(){if(!user){onAuth();return}if(user.role!=='HOST'&&user.role!=='ADMIN'){setError('Necesitás una cuenta HOST para publicar un alojamiento.');return}setBusy(true);setError('');try{const p=await api.createProperty(token,{title:form.title,description:'Alojamiento publicado desde Genesis Rentals.',propertyType:'HOUSE',city:form.city,country:form.country,address:form.city,latitude:0,longitude:0,pricePerNight:Number(form.pricePerNight),maxGuests:Number(form.maxGuests),bedrooms:1,bathrooms:1});onCreated(p);onClose()}catch(error){setError(message(error))}finally{setBusy(false)}}return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-foreground/40 p-5"><div className="w-full max-w-lg rounded-3xl bg-card p-7"><div className="flex justify-between"><div><p className="text-sm font-semibold text-primary">Para anfitriones</p><h2 className="mt-1 text-2xl font-semibold">Publicá tu espacio</h2></div><button onClick={onClose}><X/></button></div>{!user?<p className="mt-6 rounded-xl bg-secondary p-4 text-sm">Iniciá sesión con una cuenta HOST para publicar un alojamiento.</p>:<div className="mt-6 flex flex-col gap-4"><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Título del alojamiento" className="rounded-xl border border-input bg-background px-4 py-3"/><input value={form.city} onChange={e=>setForm({...form,city:e.target.value})} placeholder="Ciudad" className="rounded-xl border border-input bg-background px-4 py-3"/><div className="grid grid-cols-2 gap-3"><input value={form.pricePerNight} onChange={e=>setForm({...form,pricePerNight:e.target.value})} placeholder="Precio por noche" className="rounded-xl border border-input bg-background px-4 py-3"/><input value={form.maxGuests} onChange={e=>setForm({...form,maxGuests:e.target.value})} placeholder="Máximo de huéspedes" className="rounded-xl border border-input bg-background px-4 py-3"/></div>{error&&<p className="text-sm text-destructive">{error}</p>}<button disabled={busy} onClick={submit} className="rounded-xl bg-primary px-4 py-3 font-semibold text-primary-foreground disabled:opacity-60">{busy?'Guardando...':'Guardar alojamiento'}</button></div>}</div></div>}

function BookingsModal({onClose,token,onAuth}:{onClose:()=>void;token:string;onAuth:()=>void}){const [items,setItems]=useState<Booking[]>([]),[error,setError]=useState(''),[busy,setBusy]=useState('load');useEffect(()=>{if(!token){onAuth();return}api.bookings(token).then(r=>setItems(r.data)).catch(e=>setError(message(e))).finally(()=>setBusy(''))},[token,onAuth]);async function cancel(id:string){setBusy(id);setError('');try{const b=await api.cancelBooking(token,id);setItems(list=>list.map(x=>x.id===id?b:x))}catch(e){setError(message(e))}finally{setBusy('')}}return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-foreground/40 p-5"><div className="max-h-[86vh] w-full max-w-2xl overflow-auto rounded-3xl bg-card p-7"><div className="flex justify-between"><div><p className="text-sm font-semibold text-primary">Tus viajes</p><h2 className="mt-1 text-2xl font-semibold">Mis reservas</h2></div><button onClick={onClose}><X/></button></div>{error&&<p className="mt-4 rounded-xl bg-secondary p-4 text-sm text-destructive">{error}</p>}{busy==='load'?<p className="mt-6 text-sm text-muted-foreground">Cargando reservas...</p>:items.length?<div className="mt-6 space-y-3">{items.map(b=><div key={b.id} className="rounded-xl border border-border p-4"><div className="flex items-start justify-between gap-3"><div><b>{b.property?.title || `Reserva ${b.id.slice(0,8)}`}</b><p className="mt-1 text-sm text-muted-foreground">{b.checkIn} a {b.checkOut} · {b.guests} huéspedes · ${money(b.totalPrice)}</p><p className="mt-1 text-xs font-semibold text-primary">{b.status}</p></div>{b.status!=='CANCELED'&&b.status!=='COMPLETED'&&<button disabled={busy===b.id} onClick={()=>cancel(b.id)} className="rounded-full border border-border px-4 py-2 text-sm font-medium hover:bg-muted disabled:opacity-60">Cancelar</button>}</div></div>)}</div>:<p className="mt-6 rounded-xl bg-secondary p-4 text-sm text-muted-foreground">Todavía no tenés reservas.</p>}</div></div>}

function AdminModal({onClose,token,user}:{onClose:()=>void;token:string;user:User|null}){const [stats,setStats]=useState<AdminStats|null>(null),[users,setUsers]=useState<User[]>([]),[properties,setProperties]=useState<Property[]>([]),[bookings,setBookings]=useState<Booking[]>([]),[reviews,setReviews]=useState<Review[]>([]),[error,setError]=useState('');useEffect(()=>{if(user?.role!=='ADMIN'){setError('No tenés permisos para acceder al panel admin.');return}Promise.all([api.adminStats(token),api.adminUsers(token),api.adminProperties(token),api.adminBookings(token),api.adminReviews(token)]).then(([s,u,p,b,r])=>{setStats(s);setUsers(u.data);setProperties(p.data);setBookings(b.data);setReviews(r.data)}).catch(e=>setError(message(e)))},[token,user]);async function role(id:string, next:UserRole){try{const u=await api.changeUserRole(token,id,next);setUsers(list=>list.map(x=>x.id===id?u:x))}catch(e){setError(message(e))}}return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-foreground/40 p-5"><div className="max-h-[88vh] w-full max-w-4xl overflow-auto rounded-3xl bg-card p-7"><div className="flex justify-between"><div><p className="text-sm font-semibold text-primary">Administración</p><h2 className="mt-1 text-2xl font-semibold">Panel admin</h2></div><button onClick={onClose}><X/></button></div>{error&&<p className="mt-4 rounded-xl bg-secondary p-4 text-sm text-destructive">{error}</p>}{stats&&<div className="mt-6 grid gap-3 sm:grid-cols-4">{Object.entries(stats).map(([k,v])=><div key={k} className="rounded-xl bg-secondary p-4"><p className="text-xs text-muted-foreground">{k}</p><b className="text-xl">{v}</b></div>)}</div>}<div className="mt-6 grid gap-5 lg:grid-cols-2"><AdminList title="Usuarios">{users.map(u=><div key={u.id} className="flex items-center justify-between gap-3 border-b border-border py-2 text-sm"><span>{u.name}<br/><small className="text-muted-foreground">{u.email}</small></span><select value={u.role} onChange={e=>role(u.id,e.target.value as UserRole)} className="rounded-lg border border-input bg-background px-2 py-1"><option>GUEST</option><option>HOST</option><option>ADMIN</option></select></div>)}</AdminList><AdminList title="Propiedades">{properties.map(p=><p key={p.id} className="border-b border-border py-2 text-sm">{p.title}<br/><small className="text-muted-foreground">{p.city} · {p.status}</small></p>)}</AdminList><AdminList title="Reservas">{bookings.map(b=><p key={b.id} className="border-b border-border py-2 text-sm">{b.id.slice(0,8)} · {b.status}<br/><small className="text-muted-foreground">{b.checkIn} a {b.checkOut}</small></p>)}</AdminList><AdminList title="Reviews">{reviews.map(r=><p key={r.id} className="border-b border-border py-2 text-sm">{r.rating}/5 · {r.comment}</p>)}</AdminList></div></div></div>}
function AdminList({title,children}:{title:string;children:React.ReactNode}){return <div className="rounded-xl border border-border p-4"><h3 className="font-semibold">{title}</h3><div className="mt-3">{children || <p className="text-sm text-muted-foreground">Sin datos.</p>}</div></div>}

